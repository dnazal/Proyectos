Chateando 

<% Chat.all.each do |chat| %>
    <% if chat.user.id == current_user.id %>
        <br>
        <tr>
        <td><%= chat.user.username%> </td>
        <td><%= chat.texto%> </td>
        <td>
            <%= form_for([chat.user, chat]) do |f| %>
                <div class="field">
                <%= f.label :Texto, :class => "label"%>
                <div class="control">
                <%= f.text_field :texto, :class => "input is-rounded" %>
            </div>
            <div>
                <%= f.submit :submit %>
            </div>
            <% end %>
        <% end %>  
<% end %>  
